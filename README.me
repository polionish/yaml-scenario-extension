# Расширение для браузера (для работы в YAML-сценариями)

Пользовательский скрипт для управления устройствами и сценариями Яндекс Умного Дома через веб-интерфейс (`https://yandex.ru/iot/*`). Скрипт предоставляет улучшенный интерфейс для просмотра, импорта, экспорта, удаления и управления сценариями и устройствами с поддержкой темной и светлой тем, а также предварительного просмотра сценариев.

## Особенности

- **Просмотр данных**: Отображение списка сценариев, устройств и групп устройств Яндекс Умного Дома.
- **Импорт сценариев**: Загрузка сценариев из файлов `.yaml` или `.yml` с выбором элементов для импорта.
- **Экспорт данных**: Сохранение сценариев, устройств и групп в `.yaml`-файл с настраиваемым именем.
- **Удаление сценариев**: Удаление сценариев и устройств через интерфейс.
- **Управление состоянием**: Включение/выключение сценариев, устройств и групп.
- **Предпросмотр сценариев**: Подробный просмотр триггеров и шагов сценариев с заменой ID устройств на их имена.
- **Поддержка тем**: Адаптация интерфейса под светлую и темную темы Яндекс IoT.
- **Уведомления**: Визуальные уведомления о ходе операций (загрузка, импорт, ошибки).
- **Интерактивный интерфейс**: Таблицы с чекбоксами, кнопки для управления и модальные окна для детальной информации.

## Установка

### Требования

- Браузер с поддержкой пользовательских скриптов (например, Chrome, Firefox).
- Установленное расширение для управления скриптами, такое как [Tampermonkey](https://www.tampermonkey.net/) или [Greasemonkey](https://www.greasespot.net/).
- Доступ к веб-интерфейсу Яндекс Умного Дома (`https://yandex.ru/iot/*`).

### Установка через Tampermonkey

1. Установите расширение [Tampermonkey](https://www.tampermonkey.net/) для вашего браузера.
2. Перейдите на страницу скрипта на GitHub: [yandex-iot-script.user.js](https://github.com/<ваш_репозиторий>/raw/main/yandex-iot-script.user.js).
3. Нажмите кнопку **Install** в интерфейсе Tampermonkey.
4. Подтвердите установку скрипта.
5. Перейдите на `https://yandex.ru/iot/*`, чтобы проверить работу скрипта.

### Ручная установка

1. Скачайте файл `yandex-iot-script.user.js` из репозитория на GitHub.
2. Откройте Tampermonkey в вашем браузере.
3. Перейдите в раздел **Utilities** (или **Утилиты**).
4. Перетащите файл `yandex-iot-script.user.js` в окно Tampermonkey или выберите его через кнопку **Import**.
5. Нажмите **Install** для установки скрипта.

## Использование

После установки скрипт автоматически активируется на страницах `https://yandex.ru/iot/*`. Вы увидите новый интерфейс с панелью управления в нижней части страницы, разделённой на группы: **Мой дом**, **Экспорт**, **Импорт** и **Другое**.

### Основные функции

#### Группа "Мой дом"
- **Посмотреть**: Отображает текущие сценарии, устройства и группы, загруженные из Яндекс IoT.
- **Обновить**: Запрашивает свежие данные с сервера и обновляет отображение.

#### Группа "Экспорт"
- **Настройки**: Открывает интерфейс для выбора сценариев, устройств и групп для экспорта. Вы можете отметить нужные элементы и указать имя файла.
- **Скачать**: Экспортирует все доступные данные в `.yaml`-файл с указанным именем (по умолчанию `iot_export.yaml`).
- **Поле ввода имени файла**: Позволяет задать имя файла для экспорта (например, `my_iot_config.yaml`).

#### Группа "Импорт"
- **Настройки**: Отображает содержимое загруженного `.yaml`-файла с возможностью выбора элементов для импорта.
- **Результаты импорта**: Показывает результаты последней операции импорта, включая статус (успех/ошибка) и сообщения об ошибках.
- **Загрузить**: Открывает диалог выбора `.yaml` или `.yml`-файла для импорта.

#### Группа "Другое"
- **Очистить вкладку**: Очищает текущую вкладку и сбрасывает состояние импорта.

### Работа с таблицами
- **Чекбоксы**: Используются для включения/выключения устройств, сценариев или групп, а также для выбора элементов при импорте/экспорте.
- **Кнопка "Подробности" (📄)**: Показывает детали сценария (триггеры и шаги) или группы (список устройств и их возможности).
- **Кнопка "Удалить" (🗑️)**: Удаляет сценарий или устройство после подтверждения.
- **Статус**: Показывает состояние импорта (✔ для успеха, ✖ для ошибки) или активации (чекбокс).

### Пример использования
1. Перейдите на `https://yandex.ru/iot/*`.
2. Нажмите **Посмотреть** в группе **Мой дом**, чтобы загрузить данные.
3. В таблице сценариев нажмите **📄** рядом с нужным сценарием, чтобы просмотреть его триггеры и шаги.
4. Для экспорта выберите **Настройки** в группе **Экспорт**, отметьте нужные элементы, укажите имя файла и нажмите **Экспортировать выбранное**.
5. Для импорта нажмите **Загрузить** в группе **Импорт**, выберите `.yaml`-файл, отметьте нужные сценарии и нажмите **Импортировать выбранное**.

## Примечания
- Скрипт работает только на страницах `https://yandex.ru/iot/*` и требует авторизации в аккаунте Яндекса.
- Импорт устройств и групп в данный момент не поддерживается из-за ограничений API Яндекс IoT.
- Для корректной работы необходимо, чтобы страница Яндекс IoT полностью загрузилась перед выполнением операций.
- Ошибки импорта отображаются в таблице и в виде уведомлений. Проверьте формат `.yaml`-файла, если возникают проблемы.
- Скрипт автоматически адаптируется к темной или светлой теме интерфейса Яндекс IoT.

## Техническая информация

### Используемые технологии
- **JavaScript**: Основной язык скрипта.
- **js-yaml**: Библиотека для парсинга и генерации YAML-файлов (включена через CDN).
- **Tampermonkey API**: Для интеграции с браузером и выполнения скрипта.
- **Fetch API**: Для взаимодействия с API Яндекс IoT.
- **MutationObserver**: Для отслеживания изменений темы и структуры страницы.

### Зависимости
- `js-yaml@4.1.0` (загружается через `https://unpkg.com/js-yaml@4.1.0/dist/js-yaml.min.js`).

### Структура скрипта
- **Инициализация**: Ожидание загрузки страницы и добавление интерфейса.
- **Стилизация**: Динамическое применение стилей для светлой и тёмной тем.
- **Получение данных**: Запросы к API Яндекс IoT для получения сценариев, устройств и групп.
- **Обработка данных**: Форматирование триггеров и шагов для удобного отображения.
- **Импорт/Экспорт**: Парсинг и генерация `.yaml`-файлов.
- **Интерфейс**: Создание таблиц, кнопок, модальных окон и уведомлений.

## Разработка и отладка
Для модификации скрипта:
1. Склонируйте репозиторий или скачайте `yandex-iot-script.user.js`.
2. Откройте Tampermonkey и создайте новый скрипт или отредактируйте существующий.
3. Вставьте код из `yandex-iot-script.user.js`.
4. Для отладки используйте консоль разработчика браузера (F12).
